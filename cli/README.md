# Jimuqu DevOps CLI

Install:

```bash
npm install -g @jimuqu/devops-cli
```

Use:

```bash
jimuqu-devops login --url http://127.0.0.1:18080 --username admin
jimuqu-devops project list
jimuqu-devops apply --file jimuqu-devops.yml --trigger --watch
```
